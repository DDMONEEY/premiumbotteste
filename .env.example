# ============================================
# SESSÃO PERSISTENTE (CRÍTICO PARA VPS)
# ============================================
# Pasta onde a sessão do WhatsApp será salva
# NO VPS: /var/lib/premium-bot/auth (fora do git)
# LOCALMENTE: ./auth_info_baileys (ou deixar em branco para usar padrão)
WA_AUTH_DIR=/var/lib/premium-bot/auth

# Se = 1, DELETA a sessão toda vez que o bot inicia (NÃO USE!)
# Se = 0, PRESERVA a sessão (USE SEMPRE!)
CLEAN_SESSION_ON_START=0

# ============================================
# AMBIENTE
# ============================================
NODE_ENV=production
